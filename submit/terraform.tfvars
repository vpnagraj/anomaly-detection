## ==================== VARIABLE VALUES ====================
## copy this file to terraform.tfvars and fill in your values
## terraform.tfvars is automatically loaded by Terraform (no -var-file flag needed)
## NOTE: add terraform.tfvars to .gitignore to avoid committing sensitive values

#my_ip         = "203.0.113.42/32"   ## replace with your IP (curl ifconfig.me)
#key_pair_name = "my-key-pair"       ## replace with your EC2 key pair name
#bucket_name   = "vpn7n-ds5220-dp1"  ## replace if you need a different bucket name
#aws_region    = "us-east-1"         ## replace if deploying to a different region
